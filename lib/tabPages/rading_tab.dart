import 'package:flutter/material.dart';


class RadingsTabPage extends StatefulWidget {
  const RadingsTabPage({Key? key}) : super(key: key);

  @override
  State<RadingsTabPage> createState() => _RadingsTabPageState();
}

class _RadingsTabPageState extends State<RadingsTabPage> {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
          "Rading Page "
      ),
    );
  }
}
